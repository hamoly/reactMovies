import React from 'react';
import 'bootstrap/dist/css/bootstrap.css';

const ReleaseDate = (props) => {
return (
    <h4 className="text-primary">{props.OverviewTitle}</h4>
    )
}

export default ReleaseDate;